services:
  api:
    build:
      target: development
    volumes:
      - ./src:/app/src
      - ./nodemon.json:/app/nodemon.json
      - /app/node_modules
      - /app/dist
    command: npm run dev
    environment:
      NODE_ENV: development
      LOG_LEVEL: debug
